spring:
  application:
    name: edge-service-zuul

  sleuth:
    sampler:
      probability: 1.0

server:
  port: 8080

management:
  endpoints:
    web:
      exposure.include: '*'

zuul:
  routes:
    customers:
      url: http://localhost:8081

